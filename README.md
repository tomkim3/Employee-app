# Employee-app
Employee management

## Run application
Clone the repository and execute the following commands
```
cd Employee-app/backend
docker-compose up -d
cd ../frontend
npm install
ng serve --open
```